namespace EWC.CustomWeapon.Enums
{
    public enum StackType
    {
        Invalid = -1,
        Override, None = Override,
        Add,
        Multiply, Mult = Multiply,
        Max, Min = Max
    }
}
